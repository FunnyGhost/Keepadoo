language: node_js
node_js:
  - '9'
dist: trusty
sudo: false

services:
  - docker

cache:
  directories:
    - node_modules

before_script:
  - npm install -g @angular/cli

script:
  - yarn test
  - yarn lint
  - yarn docker-hub-build
